.DS_Store
/.build/
/.swiftpm/
/Build/
/DerivedData/
*.xcodeproj
*.xcworkspace
xcuserdata/
Pods/
Package.resolved
# Build artifacts stay out of git — distribute the app via Scripts/make-dmg.sh
# (or a GitHub Release), not by committing the binary.
/dist/
.vscode/
# Generated animation preview GIFs and reference videos
/previews/
/sharingan animation.gif
/sharingan animation.mp4
/original-*.mp4
# Retired embedded prototype (deleted from git; leftover build artifacts only)
/MoveEyes/


/docs/
# Superpowers SDD docs (specs/plans) stay local — untracked since the repo went public
/docs/superpowers/

# SDD scratch (plans, briefs, reports)
.superpowers/
