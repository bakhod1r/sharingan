.DS_Store
/.build/
/.swiftpm/
/Build/
/DerivedData/
*.xcodeproj
*.xcworkspace
xcuserdata/
Pods/
Package.resolved
# Track the built app so a second (same-arch) Mac can `git pull` and run it
# directly, but keep other build artifacts (e.g. the .dmg) out of git.
/dist/*
!/dist/Sharingan.app
.vscode/